import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import Button from '../../src/button/button'

describe('Button', () => {
    it('is ok', () => {
        expect(1).to.eq(1)
    })
})